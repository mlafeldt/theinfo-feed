[project]
name = "theinfo-feed"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["feedgen>=1.0", "feedparser>=6.0", "httpx>=0.28"]

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["scripts"]
testpaths = ["tests"]
